export enum ERole {
  MOTHER = 'mother',
  FATHER = 'father',
  TUTOR = 'tutor',
  NOT_ASSIGNED = 'not assigned',
}
export enum EUserStatus {
  ACTIVE = 'active',
  DELETED = 'deleted',
  BLOCKED = 'blocked',
}
